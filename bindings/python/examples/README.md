# In-tree Python sample

Full integration tour: create encrypted DB, create `users`, CRUD, `$and` / `$or` / `$gte` NQL. See [../README.md](../README.md).

```bash
export NUVEXA_NATIVE_LIB=/path/to/libnuvexa.dylib
python3 sample.py
```
