# Uno Platform sample

Desktop head (`net10.0-desktop`). Encrypted create, NQL, GridFS, fail-closed open. WASM is not a target — the engine needs a real filesystem.

```bash
dotnet run --project samples/Uno/UnoSample.sln -c Release
```
