<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <AssemblyName>nuvexa</AssemblyName>
    <IsPackable>false</IsPackable>
    <Description>Command-line explorer for .nvx files. CI publishes this into the VS Code / Cursor VSIX; it is not a NuGet package.</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Nuventra.NuvexaDB.Tools\Nuventra.NuvexaDB.Tools.csproj" />
  </ItemGroup>

</Project>
